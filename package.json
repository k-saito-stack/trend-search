{
  "name": "trend-search-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "X trend collector with xAI + stylish dashboard + custom themes",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "demo:seed": "node scripts/seedDemoData.js"
  },
  "engines": {
    "node": ">=20"
  }
}
